# OmniLink agent runtime dependencies.
#
# Pin the minimum library version that ships the APIs the SDK in
# `_lib/runner_base.py` calls into: OmniLinkClient.list_profiles /
# create_profile / update_profile / get_memory and UsageMeter.start /
# snapshot. v0.6.1 (2026-04 release) is the floor.
omnilink>=0.6.1

# Core HTTP client used by every bridge proxy in agent `tools/` packages.
requests>=2.31

# Image processing for perception sidecars. Optional — the smoke test
# does NOT import it, and the shipped sidecars (husky_eye) classify
# raw BGRA in pure Python rather than depend on it. Uncomment only if
# an agent you add actually needs it.
# Pillow>=10.0

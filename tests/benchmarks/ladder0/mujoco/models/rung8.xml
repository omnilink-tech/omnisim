<!-- GENERATED by tests/benchmarks/ladder0/mujoco/scenes.py.
     Ladder0 rung 8 (gripper lifts a payload off a table) -- MuJoCo arm.
     Every number here is read from the ladder's shared contract; do not
     hand-edit.  MJCF `size` is a HALF-extent where VRML `size` is a full
     extent, and `fromto` states a cylinder's axis explicitly. -->
<mujoco model="ladder0_rung8_mujoco">

  <!-- Only gravity and the timestep are set from the contract.  Everything
       else is MuJoCo's default, on purpose: this arm is the un-tuned
       reference for the solver OmniSim runs underneath. -->
  <option timestep="0.004" gravity="0 0 -9.81" noslip_iterations="5"/>

  <default>
    <!-- sliding / torsional / rolling.  condim defaults to 3, so only the
         first component is in force; the rest are declared for the record. -->
    <geom friction="3.0 0.005 0.0001"/>
  </default>

  <worldbody>
    <!-- Table: top at z=0.7.  The place target x=0.45 is inside its
         half-extent 0.6, so a DROPPED part lands on the table rather than
         on the floor -- the airborne check has to separate "carried" from
         "resting on the nearest surface", and the nearest surface is here. -->
    <geom name="table" type="box" size="0.6 0.3 0.1" pos="0 0 0.6"
          rgba="0.45 0.36 0.28 1"/>
    <geom name="floor" type="box" size="10.0 10.0 0.1" pos="0 0 0.4"
          rgba="0.4 0.42 0.45 1"/>

    <!-- The part: a 0.06 m cube of 0.2 kg, resting on the table with
         its centre at z=0.73. -->
    <body name="part" pos="0 0 0.73">
      <freejoint name="part_free"/>
      <geom name="part_geom" type="box" size="0.03 0.03 0.03" mass="0.2"
            rgba="0.85 0.72 0.25 1"/>
    </body>

    <!-- Gantry.  Rail at z=1.2; the carriage traverses along X and the
         wrist lifts along Z.  At both joints' zero the wrist origin is at
         z=0.73, which is the part's centre. -->
    <body name="carriage" pos="0 0 1.2">
      <joint name="traverse" type="slide" axis="1 0 0"/>
      <geom name="carriage_geom" type="box" size="0.06 0.06 0.04"
            mass="1.0" rgba="0.3 0.3 0.34 1"/>
      <body name="wrist" pos="0 0 -0.47">
        <joint name="lift" type="slide" axis="0 0 1"/>
        <!-- The plate hangs 0.06 m above the wrist origin, so its
             underside clears the part's top by 0.020 m. -->
        <geom name="wrist_geom" type="box" size="0.05 0.08 0.01"
              pos="0 0 0.06" mass="0.2" rgba="0.3 0.3 0.34 1"/>

        <body name="pad_l" pos="0 0.06 0">
          <joint name="pad_l_joint" type="slide" axis="0 -1 0"
                 range="0 0.048000" damping="15.4919"/>
          <geom name="pad_l_geom" type="box" size="0.015 0.006 0.025"
                mass="0.02" rgba="0.2 0.55 0.75 1"/>
        </body>

        <body name="pad_r" pos="0 -0.06 0">
          <joint name="pad_r_joint" type="slide" axis="0 1 0"
                 range="0 0.048000" damping="15.4919"/>
          <geom name="pad_r_geom" type="box" size="0.015 0.006 0.025"
                mass="0.02" rgba="0.2 0.55 0.75 1"/>
        </body>
      </body>
    </body>
  </worldbody>

  <actuator>
    <!-- The two stages are POSITION servos; kp is the larger of the droop and
         settling requirements and kv is critical damping.  run.py reports
         both explicit-Euler stability margins from the compiled model. -->
    <position name="traverse_motor" joint="traverse" kp="1743.8976"
              kv="100.2240"/>
    <position name="lift_motor" joint="lift" kp="4316.4000" kv="87.1600"/>
    <!-- The pads are POSITION servos: ctrl is the joint's commanded closure
         in metres, and the interference that develops RUNG8_GRIP_N through
         the pad-and-contact series stiffness is spec.rung8_bite_m().  kv is 0
         because the damping is in the joint -- see the note above. -->
    <position name="pad_l_motor" joint="pad_l_joint" kp="3000.0000" kv="0"/>
    <position name="pad_r_motor" joint="pad_r_joint" kp="3000.0000" kv="0"/>
  </actuator>

</mujoco>

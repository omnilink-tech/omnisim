# OmniSim PROTO schema sidecar.
# `auto:` is regenerated by `python -m omnisim proto schema`; edits there are lost.
# `extra:` is hand-curated and preserved across regeneration.
auto:
  proto: TestRectangleArena
  source: tests/protos/protos/TestRectangleArena.proto
  header:
    license: Copyright 2026 OmniLink. Apache 2.0.
    license_url: https://www.apache.org/licenses/LICENSE-2.0
    template_language: javascript
    description: Test-only arena whose `floorAppearance` is a template field, so that changing it forces the PROTO to regenerate.
  extern_protos:
  - omnisim://projects/appearances/protos/Parquetry.proto
  - omnisim://projects/objects/floors/protos/Floor.proto
  fields:
  - name: floorAppearance
    type: SFNode
    access: field
    default: Parquetry { type "chequered" }
    hint: Defines the appearance of the floor.

# OmniSim PROTO schema sidecar.
# `auto:` is regenerated by `python -m omnisim proto schema`; edits there are lost.
# `extra:` is hand-curated and preserved across regeneration.
auto:
  proto: GenericDoorAppearance
  source: projects/objects/apartment_structure/protos/GenericDoorAppearance.proto
  header:
    license: Copyright 2026 OmniLink. Apache 2.0.
    license_url: https://www.apache.org/licenses/LICENSE-2.0
    keywords: [building/component, appearance/wood]
    description: 'The finish of a painted interior door leaf: a matte, non-metallic paint with a faint brushed tooth to it,
      tinted by `colorOverride`.'
  extern_protos: []
  fields:
  - name: colorOverride
    type: SFColor
    access: field
    default: [1, 1, 1]
    hint: Defines the color to be multiplied with the texture color.

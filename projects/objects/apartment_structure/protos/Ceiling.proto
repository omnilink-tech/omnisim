#OMNISIM R2025a utf8
# license: Copyright 2026 OmniLink. Apache 2.0.
# license url: https://www.apache.org/licenses/LICENSE-2.0
# documentation url: https://github.com/omnilink-tech/omnisim/blob/main/projects/objects/apartment_structure/protos/Ceiling.proto
# keywords: interior/structure
# A flat rectangular ceiling slab. The default rotation flips the plane so that its visible face looks down into the room.

EXTERNPROTO "omnisim://projects/appearances/protos/Roughcast.proto"

PROTO Ceiling [
  field SFVec3f    translation      0 0 2.4
  field SFRotation rotation         1 0 0 3.14159
  field SFString   name             "ceiling"
  field SFString   contactMaterial  "default"
  field SFVec2f    size             10 10                                                            # Defines the size of the ceiling.
  field SFNode     appearance       Roughcast { textureTransform TextureTransform { scale 10 10 } }  # Defines the appearance of the ceiling.
  field SFBool     locked           TRUE                                                             # Is `Solid.locked`.
  field SFBool     castShadows      TRUE                                                             # Defines whether this object should cast shadows.
]
{
  Solid {
    translation IS translation
    rotation IS rotation
    children [
      Shape {
        appearance IS appearance
        geometry DEF CEILING_PLANE Plane {
          size IS size
        }
        castShadows IS castShadows
      }
    ]
    name IS name
    model "ceiling"
    contactMaterial IS contactMaterial
    boundingObject USE CEILING_PLANE
    locked IS locked
  }
}

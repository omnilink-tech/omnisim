#VRML_SIM R2025a utf8
# license: Copyright 2026 OmniLink. Apache 2.0.
# license url: https://www.apache.org/licenses/LICENSE-2.0
# documentation url: https://github.com/omnilink-tech/omnisim/blob/main/projects/objects/kitchen/breakfast/protos/BiscuitBox.proto
# keywords: household/kitchen
# A graspable carton of biscuits: a resizable cardboard box wrapped in the `textureUrl` artwork, with a tight bounding object and a mass-driven physics node. The origin sits under the box.
# template language: javascript

PROTO BiscuitBox [
  field SFVec3f    translation 0 0 0
  field SFRotation rotation    0 0 1 0
  field SFString   name        "biscuit box"
  field SFVec3f    size        0.08 0.24 0.04              # Defines the size of the box.
  field MFString   textureUrl  "textures/biscuit_box.jpg"  # Defines the texture used for the box.
  field SFFloat    mass        0.4                         # Defines the mass of the box in kg.
]
{
%<
  const size = fields.size.value;
  const mass = fields.mass.value;
  const halfZ = 0.5 * size.z;
>%
Solid {
  translation IS translation
  rotation IS rotation
  children [
    Pose {
      translation 0 0 %<= halfZ >%
      children [
        Shape {
          appearance PBRAppearance {
            baseColorMap ImageTexture {
              url IS textureUrl
            }
            roughness 0.9
            metalness 0
          }
          geometry DEF BISCUIT_BOX_GEOM Box {
            size IS size
          }
        }
      ]
    }
  ]
  name IS name
  model "biscuit box"
  recognitionColors [
    0.72 0.15 0.12
  ]
  boundingObject Pose {
    translation 0 0 %<= halfZ >%
    children [
      USE BISCUIT_BOX_GEOM
    ]
  }
%< if (mass > 0) { >%
  physics Physics {
    density -1
    mass IS mass
  }
%< } >%
}
}

# OmniSim Preferences Cleaner

The present script helps to remove the OmniSim preferences from your system.
It also removes any leftover legacy Webots preferences (from before the rebrand).

## Requirements

This script requires Python 3 to be installed and accessible from your terminal.

## Usage

```shell
python3 preferences_cleaner.py
```

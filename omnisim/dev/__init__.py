"""omnisim.dev — build, test, run, profile primitives.

These functions are importable so callers (the CLI, tests, tooling) can
invoke them without spawning a Python subprocess. The CLI in
`omnisim.cli` adds argparse on top.
"""
